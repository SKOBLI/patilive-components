{
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.3 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter storybook-app build-storybook",
  "outputDirectory": "apps/storybook/storybook-static"
}
